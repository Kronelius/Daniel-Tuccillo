# Project screenshots

Drop screenshots for the "Selected work" cards here, then uncomment the `<img>`
tag inside each card in `index.html` (and delete the placeholder `<span>`).

Expected filenames (referenced in `index.html`):

- `rainier.png`    — Rainier Facility Solutions
- `leadstart.png`  — LeadStart AI
- `saasassins.png` — SaaSassins
- `gld.png`        — GLD
- `recipal.png`    — ReciPal

Recommended size: **~1280×800** (the cards crop to a 16:10 ratio). PNG or JPG.
A clean screenshot of each site's hero/landing area works best.
